.DS_Store
# GITHUB_TOKEN lives here; see the update.py docstring.
.env
__pycache__/
*.pyc
update.log

# Regenerated on demand.
shots/

# data/raw/ and drafts/ are provenance for the narrative — keep them tracked.

# .nojekyll is intentionally tracked — it stops GitHub Pages running Jekyll.
